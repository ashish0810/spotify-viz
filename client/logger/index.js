export default class Logger {
    static debug(message) {
        console.log(message);
    }

    static info(message) {
        console.log(message);
    }

    static warn(message) {
        console.log(message);
    }
}