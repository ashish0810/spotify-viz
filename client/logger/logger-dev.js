export default class DevLogger {
    static debug(message) {
        console.log(message);
    }

    static info(message) {
        console.log(message);
    }

    static warn(message) {
        console.log(message);
    }
}