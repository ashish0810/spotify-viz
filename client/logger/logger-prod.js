export default class ProdLogger {
    static debug(message) {
        // console.log(message);
    }

    static info(message) {
        console.log(message);
    }

    static warn(message) {
        console.log(message);
    }
}